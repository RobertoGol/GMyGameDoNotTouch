# src/Progression.cpp

Задачи из `Next.md`:

- реализовать helper-логику для `relay credits`
- связать это с `SessionProfile` / `WorldFieldState`
- не смешивать сервисную экономику с чисто UI-кнопками launcher-а

Это маленький слой, но он нужен, чтобы сервисные награды и траты были частью прогрессии, а не локальной панелью.
