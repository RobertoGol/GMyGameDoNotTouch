# include/Progression.hpp

В `Next.md` этот файл фигурирует как место для маленьких helper-ов под `relay credits`.

Идея:

- не держать начисление/списание сервисной валюты размазанным по UI
- вынести простые функции прогрессии в `Progression`

Минимум, который повторялся в `Next.md`:

- helper для начисления `relay credits`
- helper для траты `relay credits`
- нормализация баланса при world/service progression
