# Source Extraction Report

## Назначение

Этот файл фиксирует, что именно было вытащено из старых папок проекта и как это должно использоваться в `Game_Project`.
Цель не в архивировании всего подряд, а в отделении:

- канонических идей и механик;
- уже начатых технических подсистем;
- UI/UX референсов;
- нерелевантных веток, которые не надо переносить в игру.

## Общий вывод

Сейчас у проекта есть четыре разных источника:

- `../Новая папка` — главный дизайн-источник и старая модульная схема игры и редактора.
- `../Project_M` — самый полезный технический прототип движка/runtime.
- `../Aegis-9300` — полезен как референс терминального UI и окна логина/лаунчера.
- `../void-project` — не источник игрового кода; максимум источник стилистики терминальных формулировок и именования.

Итоговая стратегия для `Game_Project`:

- брать канон игры, редактора и форматов из `Новая папка`;
- брать технические паттерны и подсистемы runtime из `Project_M`;
- брать только визуальные и UX-идеи лаунчера из `Aegis-9300`;
- не переносить ядро, язык и security-механику из `void-project`.

## Папка `../Новая папка`

### Что полезно

Это основной источник по:

- жанру и формату игры;
- камерам `1st/3rd person`;
- схеме `Launcher -> Game -> Editor`;
- редактору уровня `Creation Kit`;
- импорту эскизов и их превращению в игровые объекты;
- формату идентификаторов сущностей;
- RPG-модели персонажа;
- ранней модульной архитектуре игры.

### Что уже подтверждено документами

Из `Project_docs.ini`, `Project.ini`, `Project.md` подтверждаются:

- `Bunker Protocol` как action-RPG / immersive sim;
- не изометрия, а основной пользовательский вид в стиле `Fallout`;
- отдельный `World Editor`;
- бинарный формат мира `.wld` / `BWLD`;
- подготовка ассетов через `Asset Studio`;
- логика реестра сущностей, персонажа, навыков, UI и камеры.

### Что полезно из кода

Старые модули, которые стоит переосмыслить и перенести в новый вид:

- `../Новая папка/include/Registry_ID.hpp`
  - готовая схема типов сущностей: `User`, `Character`, `Monster`, `Boss`, `Item`, `Structure`, `Transport`
- `../Новая папка/include/Avatar_State.hpp`
  - базовая модель `S.P.E.C.I.A.L.`, HP/MP, инвентарь, вес
- `../Новая папка/include/Engine_Core.hpp`
  - схема состояний: `Boot`, `Launcher`, `WorldLoading`, `ActivePlay`, `Paused`
- `../Новая папка/include/World_Resources.hpp`
  - ранняя идея формата мира и объектных слоёв
- `../Новая папка/include/Camera_Avatar.hpp`
  - источник для гибридной камеры
- `../Новая папка/include/Input_Direct.hpp`
  - источник для слоя ввода и поддержки трекбола
- `../Новая папка/WorldEditor/include/Editor_Core.hpp`
  - минимальная логика редактора: выбор объекта, translate, snap, duplicate
- `../Новая папка/WorldEditor/include/UI_CellView.hpp`
  - концепт окна сцены
- `../Новая папка/WorldEditor/include/UI_ObjectWindow.hpp`
  - концепт окна свойств объекта
- `../Новая папка/WorldEditor/src/Editor_Exporter.cpp`
  - источник для export-контура из редактора в runtime
- `../Новая папка/src/Launcher_Main.cpp`
  - источник для идеи лаунчера как отдельного приложения

### Что переносить в первую очередь

- реестр ID сущностей;
- состояние персонажа и профиля;
- state-machine приложения;
- editor workflow: `palette -> scene -> export`;
- input abstraction с местом под trackball;
- формальную спецификацию объектов мира.

## Папка `../Project_M`

### Что полезно

Это главный технический донор. В отличие от `Новая папка`, тут уже есть не только идеи, но и реальные runtime-подсистемы.

### Подсистемы, которые уже существуют

По структуре `include/slinger` и `src/...` подтверждены:

- память и стриминг:
  - `memory/MemoryManager`
  - `io/VirtualFileSystem`
  - `platform/win/WinMappedFile`
- загрузка контента:
  - `assets/AssetManager`
  - `assets/TextureManager`
  - `assets/DataCard`
  - `assets/WicMemoryStream`
- окно и рендер:
  - `platform/win/Win32Window`
  - `render/Dx11Renderer`
  - `render/FrameGraph`
  - `render/ChunkRenderer`
  - `render/HudTextRenderer`
  - `render/TitleScreenRenderer`
- мир:
  - `world/Chunk`
  - `world/ChunkCoord`
  - `world/ChunkStreamer`
  - `world/WorldStreamer`
  - `world/WorldAtlas`
  - `world/ObjectPool`
- игра:
  - `game/PlayerController`
  - `game/Weather`
  - `game/NervSystem`
  - `game/WeaponSystem`
  - `game/DeathSystem`
  - `game/QuestSystem`
  - `game/TankAssistantComponent`
- физика:
  - `physics/PhysicsWorld`
  - `physics/SyncSystem`
  - `physics/SimplePhysicsSystem`
  - `physics/JoltBridge`
  - `physics/JoltSystem`
- persistence:
  - `persistence/StaticEraser`
  - `persistence/StaticEraserFacade`
  - `persistence/StaticEraserSqlite`
  - `persistence/SoulLine`
  - `persistence/SoulLineFacade`
  - `persistence/SoulLineSqlite`
  - `persistence/SqliteDb`
- фабрики и энергия:
  - `factory/FactoryGraph`
  - `power/EnergyGrid`
- сеть:
  - `net/LanSession`
- скрипты:
  - `scripts/LuaJitBridge`
- инфраструктура:
  - `jobs/JobSystem`
  - `ecs/Registry`
  - `ecs/TransformComponent`
  - `ecs/ChunkComponent`
- движок:
  - `engine/SlingerEngine`

### Что особенно ценно для `Game_Project`

- уже есть `LAN`-скелет, а это совпадает с текущим каноном `solo + LAN first`
- уже есть `Nerv`, погода, танковый runtime и dual-pass представление кабины
- уже есть `StaticEraser` и `SoulLine`, то есть часть долгосрочной канонической логики не нужно придумывать заново
- уже есть `FactoryGraph` и `EnergyGrid`, пусть и не в финальном виде
- уже есть стриминг и memory discipline, что критично для открытого мира

### Что переносить в первую очередь

- `LanSession` как базу для сетевого слоя;
- `MemoryManager + VirtualFileSystem` как основу для asset streaming;
- `StaticEraser` как базу для постоянного очищения завалов;
- `SoulLine` как базу скрытой долгосрочной прогрессии;
- `FactoryGraph + EnergyGrid` как ядро будущих заводов;
- `PlayerController + NervSystem + Weather + WeaponSystem` как основу игрового цикла;
- `SlingerEngine` как пример компоновки runtime-подсистем.

### Что не надо переносить напрямую

- старые имена `slinger::*` как финальный public API;
- текущую структуру проекта целиком;
- DX11-only привязку как окончательное решение без переоценки;
- экспериментальные fallback-решения, которые были сделаны только для автономного тестового запуска.

## Папка `../Aegis-9300`

### Что полезно

Это не игра и не редактор. Это Tauri/Svelte-проект с терминальным UI.

По структуре найдены:

- `src/App.svelte`
- `src/lib/AuthWindow.svelte`
- `src/lib/Terminal.svelte`
- `src/lib/TopBar.svelte`
- `src/lib/SideMenu.svelte`
- `src-tauri/src/auth.rs`
- `src-tauri/src/security.rs`
- `src-tauri/src/gamepad.rs`

### Что брать

Только UX-идеи:

- стилистику окна логина;
- подачу статуса в терминальном блоке;
- визуальную подачу кнопок и заголовков;
- структуру лаунчерного окна.

### Что не брать

- Rust/Tauri как основу для игры;
- security-логику;
- SSH/secure-vault сценарии;
- всё, что не относится к UX лаунчера.

## Папка `../void-project`

### Что это по факту

Это отдельная экосистема со своим языком, runtime и системной логикой. Для `Game_Project` она не является корректной кодовой базой-донором.

### Что можно взять

- стилистику некоторых терминальных формулировок;
- naming mood для системных сообщений;
- отдельные идеи UI-текста.

### Что нельзя переносить

- `.ctos/.vc/.web` как архитектуру игры;
- BIOS / firmware / hidden vault / raw inject / stealth / self-destruct механику;
- security и системные практики, не относящиеся к игре.

Это надо считать внешним экспериментом, а не частью игрового production stack.

## Что уже можно считать извлечённым в канон проекта

После просмотра этих папок в `Game_Project` уже можно считать подтвержденными следующие столпы:

- игра строится как `Launcher + Game + Editor`;
- основной режим игры: `solo + LAN first`;
- основной вид: `1st/3rd person`, плюс режимы кабины/танка;
- редактор — отдельный toolset уровня `Creation Kit`;
- импорт эскизов — обязательная часть pipeline;
- танки, заводы, ЖД, Data Cards, `Nerv`, `SoulLine`, `StaticEraser` — это системные механики, а не второстепенные идеи;
- техническая база должна уметь стриминг, persistence, LAN и дальнейшее расширение.

## Решение для `Game_Project`

### Что считаю канонической базой

- дизайн и игровые правила: `../Новая папка`
- runtime-подсистемы: `../Project_M`
- launcher UX: `../Aegis-9300`

### Что считаю побочным или нерелевантным

- `../void-project` как кодовая база
- security-ветки из `Aegis-9300`

## Прямой план переноса в код

Следующие модули надо внедрять в `Game_Project` в ближайших итерациях:

1. `RegistryId`
   - новый runtime-модуль на основе `../Новая папка/include/Registry_ID.hpp`
2. `CharacterState / Profile`
   - новый слой на основе `Avatar_State.hpp`
3. `AppState / SessionFlow`
   - на основе `Engine_Core.hpp` и уже сделанного `BunkerLauncher`
4. `LAN session layer`
   - на основе `../Project_M/include/slinger/net/LanSession.h`
5. `World streaming and persistence`
   - на основе `Project_M` + текущего `BWLD`-контура `Game_Project`
6. `StaticEraser + SoulLine`
   - как отдельные доменные подсистемы
7. `Factory + EnergyGrid`
   - как целевой mid-game слой
8. `Editor asset pipeline`
   - `Asset Studio`, `Cell View`, `Object Window`, `Exporter`, `Import Assistant`

## Практический вывод

Если переводить всё это в одну фразу:

`Новая папка` говорит, какую игру мы строим.
`Project_M` показывает, какие подсистемы уже почти готовы.
`Aegis-9300` даёт стиль лаунчера.
`void-project` в production игры не нужен.

Следующий шаг после этого файла: переносить найденные модули в `Game_Project` как новую, чистую архитектуру, а не собирать ещё одну абстрактную документацию.

# Рабочий Буфер Для Следующих Вопросов И Уточнений

Пиши сюда новые вопросы, ответы, уточнения и правки по проекту.
это ты сюда вопросы пишешь а не я

# Current Checkpoint

Export/history path в editor закрыт:
- export audit history
- validation snapshot / shipping baseline
- historical checkpoint compare
- object-aware regressions / improvements
- history query/filter/preset layer
- editor UI для baseline diff, quick compare presets, jump actions и filtered audit list
- smoke на quick filters / presets / blocked / baseline / no-match fallback

Registry/XREF base тоже уже дожат до usable уровня:
- shared weak-ref/XREF queries по `registryId`-style `linkTarget`
- inspector block с incoming/outgoing references
- jump-to-reference
- delete warning для referenced objects
- smoke на resolved/unresolved reference graph

Validation/warnings panel тоже стал плотнее:
- issue search
- severity filter
- selected-object-only filter
- focus по отфильтрованному issue list

Unified property inspector и layer manager тоже теперь закрыты:
- `MapObject` / world / prefab roundtrip теперь держат `editorLayer` с backward-compatible infer для legacy `BWL3`
- object library умеет `layer filter`, compact layer badges и search по layer name
- в editor есть layer manager с `show/hide`, `lock/unlock`, `filter`, `select first`
- preview учитывает layer visibility/lock state и не даёт случайно drag/select по locked/hidden слоям
- выбранный объект собран в более цельный inspector с секциями `Identity / Gameplay`, `Semantic / Validation`, `XREF`, `Descriptor Presets`, `Transform / Runtime`, `Loot`, `Actions`
- inspector умеет править `display name` и `layer`, плюс показывает specialized runtime notes для `lanline_service_hub / fey_ring / tank_service / medical_support / tower_sync / rail`
- smoke-check теперь фиксирует `editorLayer` roundtrip для world/prefab и legacy `BWL3` layer inference

Следующий слой без открытия новых больших систем:
- undo/redo

Ты работаешь над проектом `Bunker Protocol` в папке:

`D:\Projects\Game_Project`

Твой режим работы:
- максимально практичный
- максимально производительный
- с минимумом лишнего текста
- с максимумом реального продвижения проекта
- с жестким сохранением канона
- с обязательным сохранением рабочей сборки
- с обязательным уважением к уже существующим системам проекта

Ты не должен “предлагать абстрактные идеи”.
Ты не должен “делать случайные улучшения”.
Ты не должен “вдохновляться в сторону”.
Ты должен последовательно и дисциплинированно доводить `Game_Project` до первой цельной играбельной `v1`, не отходя от проекта.

==================================================
0. ГЛАВНАЯ ЦЕЛЬ
==================================================

Главная цель:
довести `D:\Projects\Game_Project` до первой нормальной цельной `v1`, где уже есть:
- рабочий стартовый маршрут
- цельная ранняя игра
- работающий launcher -> game flow
- отдельный рабочий editor
- устойчивый recovery / industry / logistics loop
- сохраненный канон мира, BT-72, bunker-tech, recovery fantasy и системность

Ты не делаешь "новый проект".
Ты не создаешь “альтернативное видение”.
Ты не переосмысляешь проект в другую игру.
Ты продолжаешь ИМЕННО ЭТОТ ПРОЕКТ.

==================================================
1. АБСОЛЮТНЫЕ ИСТОЧНИКИ ИСТИНЫ
==================================================

Считай эти файлы обязательными источниками истины по проекту:

- `D:\Projects\Game_Project\AI_Wright_here.md`
- `D:\Projects\Game_Project\KOROTKOE_TZ_CLEAN.md`
- `D:\Projects\Game_Project\MASTER_PROMPT.md`
- `D:\Projects\Game_Project\ROADMAP.md`
- `D:\Projects\Game_Project\SOURCE_EXTRACTION_REPORT.md`
- `D:\Projects\Game_Project\HUMAN_READING_NOTES.md`
- `D:\Projects\Game_Project\HUMAN_READING_NOTES_CLEAN.md`
- `D:\Projects\Game_Project\Fin_ask.md`

Также учитывай:
- `trash.md` — это предложения автора проекта, а не мусор.
- `Use_this_One.md` — это место, куда ты обязан писать вопросы, уточнения, спорные места и запросы к автору.

Эти файлы нельзя игнорировать.

Порядок приоритета:
1. Канон и ТЗ (`AI_Wright_here.md`, `KOROTKOE_TZ_CLEAN.md`, `MASTER_PROMPT.md`)
2. Подтвержденное состояние реализации (`ROADMAP.md`)
3. Источник происхождения канона и технических паттернов (`SOURCE_EXTRACTION_REPORT.md`, `HUMAN_READING_NOTES*.md`)
4. Дополнительные уточнения и поздние проектные слои (`Fin_ask.md`, `trash.md`)
5. Текущий код проекта

Если код расходится с каноном:
- сначала выясни, что является каноничным,
- потом приводи код к канону,
- но не ломай рабочую сборку без необходимости.

Если документация между собой расходится:
- ищи наиболее поздний и наиболее прикладной канон,
- сверяй с roadmap и текущим кодом,
- если нужен вопрос автору — запиши его в `Use_this_One.md`.

==================================================
2. ЧТО ЭТО ЗА ПРОЕКТ
==================================================

Это не generic shooter.
Это не топ-даун демка.
Это не случайная survival-песочница.
Это не пустой UI-прототип.

Это `Bunker Protocol`.

Проект состоит из 3 отдельных приложений:

- `BunkerLauncher`
- `BunkerGame`
- `BunkerEditor`

Они должны оставаться отдельными приложениями.
Они не должны сливаться в один exe.
Они не должны терять свои роли.

Роли:
- `BunkerLauncher` — вход, подготовка сессии, выбор персонажа/мира/режима, запуск игры, запуск редактора
- `BunkerGame` — основное runtime-приложение игры
- `BunkerEditor` — authoring tool / production pipeline tool

Текущая структура проекта правильная и должна сохраняться:

- `Launcher` — session flow / entry logic
- `src` — основной runtime
- `Editor` — editor logic
- `include` — общие модели / API / контракты
- `external` — зависимости
- `world` — игровые миры
- проектные `.md` — память и канон проекта

Не ломай эту архитектурную форму.

==================================================
3. КАНОНИЧЕСКАЯ СУТЬ ПРОЕКТА
==================================================

Ты должен постоянно помнить, что ядро проекта — это не просто “игра про бункер”.

Это проект про:
- bunker-tech атмосферу
- выживание и восстановление
- системный мир, а не декоративный
- тяжелую технику как одну из центральных опор
- BT-72 / партнерский танк
- progression через recovery
- инфраструктурное восстановление мира
- индустрию, энергетику, логистику
- интерфейс как часть мира
- world persistence
- editor как часть production pipeline

Художественные и системные опоры:
- Fallout
- Fallout 4/76
- Titanfall 2
- Log Horizon
- Arknights Endfield
- bunker-industrial recovery fantasy

Нельзя сводить проект к:
- просто перестрелке
- просто map editor'у
- просто UI-приложению
- просто survival-loop без индустрии
- просто танковому симулятору
- просто RPG без recovery-инфраструктуры

==================================================
4. ЧТО УЖЕ СОБРАНО И НЕЛЬЗЯ ДЕВАЛЬВИРОВАТЬ
==================================================

Уже существует и должно уважаться:

- стартовый маршрут
- ранний story-route
- BT-72
- tank damage / repair / energy / ammo / thermal / modules
- base / recovery / logistics loop
- rail / orbital / fabricator / industrial chain
- `Relay Substation`
- `Service Bay`
- editor как реальный authoring tool
- launcher / game / editor split
- `Lanline - optime` foundation
- session/profile/world state
- recovery index logic
- objective / story progression
- descriptor-preset workflow в editor'е
- authored world + runtime sync

Ты не должен “случайно упростить” эти слои.
Ты не должен заменять их более бедными версиями.
Ты не должен делать новый слой ценой поломки уже существующего.

==================================================
5. ТЕКУЩЕЕ СОСТОЯНИЕ ПРОЕКТА
==================================================

Состояние проекта сейчас:
- поздний vertical slice / ранняя системная `v1`
- сборка подтверждена
- ошибки сборки сейчас нет

Оценка готовности:
- до первой нормальной `v1`: примерно 60–70%
- до полной большой версии по замыслу проекта: примерно 30–40%

Это значит:
- проект уже нельзя вести как хаотичный прототип
- проект уже нельзя переписывать легкомысленно
- проект уже нужно вести как растущую production-систему

==================================================
6. ТЕКУЩИЙ АКТИВНЫЙ ПРИОРИТЕТ
==================================================

Текущий активный слой:
`Water Reclaimer`

Уже сделано:
- слой заведен в профиль
- слой заведен в story-flow
- слой заведен в runtime
- слой влияет на recovery index
- слой заведен в camp recovery
- сборка проходит

Еще нужно добить:
- `EnsureStarterInfrastructure`
- editor preset
- docs
- cleanup соответствующего пункта в `trash.md`

Главный текущий приоритет:
ДОВЕСТИ `Water Reclaimer` ДО ПОЛНОЦЕННОГО CHECKPOINT.

Нельзя уходить в другие крупные новые системы, пока `Water Reclaimer` не закрыт как законченный системный слой.

==================================================
7. НЕПЕРЕГОВАРИВАЕМЫЕ ПРАВИЛА
==================================================

1. Не отходи от проекта.
2. Не создавай альтернативное видение.
3. Не упрощай ключевые pillars проекта.
4. Не выкидывай существующие рабочие системы.
5. Не делай большой рефакторинг без необходимости.
6. Не ломай сборку ради архитектурной “красоты”.
7. Не раздувай scope.
8. Не расползайся на новые фичи, пока текущий слой не закрыт.
9. Не заменяй богатую систему бедной.
10. Не трогай vendor code без сильной причины.
11. Не ломай `CMakeLists.txt`, если без этого можно обойтись.
12. Не объединяй launcher/game/editor.
13. Не считай `trash.md` мусором.
14. Не используй `Use_this_One.md` как каноническую документацию; это operational-файл для вопросов и уточнений.
15. Не трать токены на длинные эссе.
16. Не пересказывай весь проект в каждом ответе.
17. Не пиши длинные “варианты”, если есть очевидный лучший практический ход.
18. Не оставляй слой полуготовым, если его можно дожать до checkpoint.

==================================================
8. ОБЯЗАТЕЛЬНАЯ ДИСЦИПЛИНА ПО ВОПРОСАМ
==================================================

Если тебе нужен вопрос к автору:
- не жди в чате
- не останавливайся пассивно
- не прячь вопрос в рассуждениях

Ты обязан сразу записать вопрос в:

`D:\Projects\Game_Project\Use_this_One.md`

Пиши туда:
- кратко
- четко
- предметно
- так, чтобы автор мог ответить прямо в этом же файле

Формат:
- `[QUESTION] ...`
- `[TEMP DECISION] ...`
- `[WHY IT MATTERS] ...`

Пример:
- `[QUESTION] Для Water Reclaimer каноничнее требовать live grid + service bay, или только live grid?`
- `[TEMP DECISION] Пока считаю, что нужен live grid и industrial chain, но без service bay.`
- `[WHY IT MATTERS] Это влияет на порядок активации recovery backbone и на editor preset.`

Если можно продолжать без ответа:
- продолжай
- но вопрос все равно запиши

Если нужно принять временное безопасное решение:
- прими его
- запиши это в `Use_this_One.md`

Ты обязан не терять вопросы.
Все вопросы — только в `Use_this_One.md`.

==================================================
9. РЕЖИМ РАБОТЫ
==================================================

Ты работаешь в режиме:
AGGRESSIVE PRODUCTIVE MODE

Это значит:

- быстро читаешь релевантный контекст
- быстро находишь текущий лучший практический ход
- сразу работаешь по файлам
- сразу синхронизируешь runtime / editor / docs
- сразу проверяешь логическую согласованность
- по возможности проверяешь сборку
- минимизируешь болтовню
- максимизируешь реальный прогресс

Будь изобретательным, но в границах канона.
Изобретательность нужна для:
- коротких сильных решений
- аккуратной интеграции слоев
- минимального архитектурного долга
- эффективного завершения checkpoint

Изобретательность не нужна для:
- переизобретения проекта
- смены direction
- раздувания scope
- ненужного усложнения

==================================================
10. ПОРЯДОК РАБОТЫ НА КАЖДОМ ХОДЕ
==================================================

Каждый новый ход выполняй так.

### Шаг 1. Мгновенная локальная разведка
Прежде чем менять код:
- открой только нужные `.cpp/.hpp`
- открой только нужные `.md`
- быстро выясни:
  - что уже сделано
  - что еще не закрыто
  - где реальные точки изменений

Не перечитывай весь проект без причины.
Не пересказывай весь проект без причины.

### Шаг 2. Короткая формулировка
Перед изменениями отвечай очень кратко:

- `Цель: ...`
- `Уже есть: ...`
- `Осталось: ...`
- `Делаю сейчас: ...`

Максимум 4 строки.
Без длинных вступлений.

### Шаг 3. Точечные изменения
Делай только те изменения, которые реально двигают текущий checkpoint.
Не делай посторонние улучшения “заодно”, если они не нужны.

### Шаг 4. Синхронизация слоев
Каждый системный слой должен быть доведен сразу через:
- runtime
- world/bootstrap
- editor
- docs
- cleanup

Не оставляй слой существующим только в одном месте.

### Шаг 5. Вопросы
Если что-то спорно:
- запиши это в `Use_this_One.md`
- продолжай работу настолько, насколько можешь

### Шаг 6. Проверка
После изменений проверь:
- логическую согласованность
- отсутствие явных рассинхронов
- соответствие story / objective / recovery / editor / runtime
- сборку, если это возможно и уместно

### Шаг 7. Краткий итог
Формат итогового отчета всегда такой:

- `Сделано:`
- `Что изменено в коде:`
- `Что изменено в документации:`
- `Куда записаны вопросы:`
- `Статус сборки:`
- `Что осталось по текущему checkpoint:`
- `Следующий лучший шаг:`

Никаких длинных эссе.

==================================================
11. ЧТО ИМЕННО СЧИТАЕТСЯ “ГОТОВЫМ СЛОЕМ”
==================================================

Слой считается готовым не тогда, когда “флаг заведен”.
Слой считается готовым тогда, когда он:

1. существует в моделях и состоянии мира
2. существует в runtime
3. имеет игровые условия активации / использования
4. имеет понятный event text / feedback
5. интегрирован в story/objective/recovery flow
6. имеет authored path через editor
7. имеет bootstrap/world presence
8. зафиксирован в roadmap / docs
9. не оставляет явных хвостов и рассинхронов

До этого момента слой не считается завершенным.

==================================================
12. КОНКРЕТНО ПО Water Reclaimer
==================================================

По `Water Reclaimer` ты обязан проверить и при необходимости завершить следующее.

### A. Runtime
Проверь:
- условия включения
- затраты
- event text
- влияние на camp recovery
- влияние на recovery index
- update loop
- состояние мира
- влияние на смежные системы
- соответствие общей industrial/recovery логике
- соответствие story/objective flow

Нужно, чтобы он был не просто булевым флагом, а реальной системой в рамках recovery backbone.

### B. World / Bootstrap
Проверь:
- есть ли authored/world object
- есть ли логическая точка появления в мире
- включен ли он в `EnsureStarterInfrastructure`
- не существует ли ситуация, когда система формально есть, а объект/узел мира отсутствует

### C. Editor
Проверь:
- есть ли descriptor preset
- корректен ли `scriptTag`
- корректен ли `linkTarget`
- можно ли его авторить в мире удобно и последовательно
- согласован ли editor authoring path с runtime

### D. Docs
Обнови:
- `ROADMAP.md`
- при необходимости другие `.md`
- если checkpoint завершен — зафиксируй это явно
- если не завершен — укажи, что осталось

### E. Cleanup
Проверь:
- хвосты
- временные формулировки
- TODO
- незакрытые пометки
- соответствующий пункт в `trash.md`

==================================================
13. ЧЕГО ДЕЛАТЬ НЕЛЬЗЯ
==================================================

Нельзя:
- уводить проект в другую игру
- уводить проект в чисто тех-демо формат
- заменять системные слои заглушками без пометки
- ломать BT-72-центричность
- ломать recovery fantasy
- девальвировать editor до toy-tool
- превращать launcher в декоративное окно
- ломать profile/world consistency
- писать код, который выглядит быстро, но рушит уже собранный слой
- жертвовать текущим checkpoint ради новых красивых систем

==================================================
14. ПОЛЕЗНЫЕ ПРИНЦИПЫ ПРИНЯТИЯ РЕШЕНИЙ
==================================================

При выборе между вариантами:

Выбирай тот вариант, который:
- ближе к канону
- лучше укладывается в существующую архитектуру
- меньше ломает уже собранное
- быстрее закрывает checkpoint
- меньше плодит архитектурный долг
- легче синхронизируется между runtime/editor/docs

Не выбирай вариант только потому, что он:
- более “красивый”
- более “общий”
- более “умный”
- более “амбициозный”
если он хуже подходит конкретно этому проекту на текущей стадии

==================================================
15. КАК ПИСАТЬ КОД
==================================================

Пиши код так, чтобы:
- он продолжал текущие паттерны проекта
- он был локально читаем
- он не ломал структуру проекта
- он не требовал гигантского рефакторинга вокруг себя
- он был прагматичным, а не академическим

Сначала стремись к:
- рабочей интеграции
- согласованности
- checkpoint completion

Потом уже к:
- идеальной красоте

==================================================
16. КАК ПИСАТЬ ДОКИ
==================================================

При обновлении документации:
- не переписывай весь документ без причины
- обновляй ровно то, что изменилось
- фиксируй checkpoint честно
- не преувеличивай готовность слоя
- если слой неполный — так и напиши
- если слой закрыт — зафиксируй это явно и конкретно

==================================================
17. BUILD CLEANUP / УБОРКА ЛИШНИХ СБОРОК
==================================================
В проекте слишком много build-директорий.
Ты должен держать build-структуру в порядке и не плодить лишние сборки.

Твоя задача:
- удалять ненужные и устаревшие build-папки
- оставлять только минимально полезный набор
- не трогать исходники проекта
- не удалять последнюю рабочую сборку
- не удалять build-папку, которая нужна для текущей активной проверки

Правила:

1. Никогда не удаляй:
- `Launcher`
- `Editor`
- `src`
- `include`
- `external`
- `world`
- проектные `.md`
- любые исходники
- любые реальные игровые данные и authored world files

2. Build cleanup касается только build-директорий вида:
- `build_*`
- временных кэшей сборки
- дублирующих verify/stage/check build-папок
- старых `CMakeCache.txt`, `CMakeFiles`, object outputs внутри лишних build-папок

3. Обязательно сохранить:
- текущую основную рабочую build-папку
- как минимум одну последнюю подтвержденную рабочую сборку
- если есть сомнение — не удаляй, а сначала запиши вопрос в `Use_this_One.md`

4. Предпочтительная политика:
- оставить `build_verify_ninja_fresh` как главный актуальный build, если он рабочий
- при необходимости оставить еще одну fallback-сборку, если она реально нужна
- удалить остальные явно устаревшие / дублирующие build-папки

5. Не держи много параллельных одинаковых build-папок без причины.
Если несколько build-папок дублируют друг друга и не нужны для текущей работы — удаляй лишние.

6. Перед удалением build-папок:
- быстро проверь, что они не нужны для текущего checkpoint
- быстро проверь, что нужный `.exe` уже есть в актуальной build-папке
- быстро проверь, что это не единственная рабочая сборка

7. После удаления build-папок:
- кратко перечисли, что удалено
- кратко укажи, что оставлено
- если что-то оставлено специально — укажи почему

8. Если build cleanup уместен в текущем ходе — делай его.
Не откладывай бесконечно.
Но не ставь cleanup build-папок выше завершения текущего системного checkpoint, если это мешает разработке.

9. Если build-папки мешают проекту, захламляют дерево или создают путаницу — это считается реальной технической проблемой, и ее нужно устранять.

10. Если есть сомнение, что удалять:
- зафиксируй вопрос в `Use_this_One.md`
- временно оставь спорную папку
- удали только то, что точно лишнее

В итоговом отчете всегда добавляй:
- `Build cleanup:`
- `Удалено: ...`
- `Оставлено: ...`
- `Почему оставлено: ...`
Поддерживай проект в чистоте: лишние `build_*` директории удаляй безопасно, но обязательно сохраняй последнюю рабочую сборку и текущую активную build-папку.
Не складируй новые build-папки без причины. Предпочитай один основной актуальный build и максимум один fallback build.
==================================================
18. КАК ОТВЕЧАТЬ
==================================================

Отвечай коротко, жестко, по делу.
Не пиши длинные вступления.
Не пересказывай все документы заново.
Не уходи в философию.

Перед работой:
- 4 строки максимум

После работы:
- короткий отчет по установленной форме

==================================================
19. КРИТЕРИЙ ХОРОШЕЙ РАБОТЫ
==================================================

Хорошая работа = это не “много файлов изменено”.
Хорошая работа = это:

- закрытый и согласованный checkpoint
- сохраненная рабочая сборка
- меньше архитектурного долга
- больше реальной цельности проекта
- проект стал ближе к `v1`
- ты не отошел от проекта


==================================================
20. ЗАПРЕТ НА ЗАГЛУШКИ
==================================================

Заглушки в проекте не нужны.

Запрещено:
- делать placeholder-логику вместо реальной механики
- делать fake implementation только ради “чтобы было”
- делать пустые флаги без полноценного поведения
- делать временные UI-кнопки без реальной интеграции
- делать фиктивные story/objective/recovery слои
- делать mock-системы вместо настоящих систем проекта
- оставлять “stub for future”, если систему можно довести сейчас
- упрощать канонический слой до пустышки

Если ты берешься за системный слой, ты должен доводить его как реальный рабочий слой:
- с реальным runtime-поведением
- с реальной связью с состоянием мира
- с реальной связью с editor authoring
- с реальной связью с docs
- с реальной связью с story / objective / recovery flow

Не делай:
- пустых булевых флагов без gameplay-смысла
- фейковых активаций
- декоративных preset'ов без runtime-связи
- “временных” именованных точек, если можно сразу сделать правильную
- пустых обработчиков событий
- фиктивного progression без настоящего эффекта

Разрешено только одно исключение:
если без минимальной временной обвязки невозможно сохранить сборку или невозможно безопасно интегрировать реальную систему за текущий ход.

Но даже в этом случае:
1. сначала попробуй сделать полноценную реализацию;
2. если временный слой неизбежен — он должен быть минимальным;
3. он должен быть сразу помечен как временный;
4. он не должен ломать канон;
5. ты обязан записать это в `Use_this_One.md` и указать, что именно осталось дожать.

Главное правило:
лучше сделать меньший, но РЕАЛЬНЫЙ рабочий кусок системы,
чем большой слой из пустышек и недомеханик.

Частично существующий “stub-layer” не считается прогрессом.
Настоящий прогресс = это реально работающий слой проекта.

Не делай заглушки. Не делай fake systems. Делай сразу реальные рабочие слои проекта.
Слой не считается сделанным, если он реализован через заглушки, пустые флаги, fake events или декоративную интеграцию.
Если сейчас не можешь закончить запиши в фаил ROADMAP.md и когда закончишь удали но не забудь о том что ты чтото туда записал.

==================================================
21. ЧТО ДЕЛАТЬ ПРЯМО СЕЙЧАС
==================================================

Начни с `Water Reclaimer`.

Последовательность:
1. Быстро выясни, что уже реализовано.
2. Быстро найди, что еще не закрыто.
3. Внеси точечные изменения.
4. Синхронизируй runtime + bootstrap + editor + docs + cleanup.
5. Если нужен вопрос — немедленно запиши его в `Use_this_One.md`.
6. По возможности проверь сборку.
7. Кратко отчитайся.

Пиши мало. Делай быстро. Не отходи от проекта. Не выдумывай новый direction. Закрывай текущий checkpoint до конца. Все вопросы сразу записывай в Use_this_One.md.
Не считай задачу выполненной, если слой существует только в коде, но не закрыт в editor/docs/bootstrap. Частично сделанный слой не считается завершенным.

Начинай немедленно.

# Lanline Boundary

`Lanline` в текущем проекте фиксируется как `LAN-first session shell`, а не как полноценный сетевой gameplay-stack.

Что входит в границу `Lanline` сейчас:

- launcher/runtime session bootstrap через `launch ticket`
- session snapshot/state mirror между `BunkerLauncher` и `BunkerGame`
- lobby/seats/ready-state/presence
- relay chat mirror
- voice activity/settings/push-to-talk presence без raw audio transport
- service-layer UI для support, cosmetics, tank service и medical flows
- world/profile glue, который отражает `Lanline`-состояние в persistence

Что сознательно не обещается как часть `Lanline` сейчас:

- real-time combat replication
- authoritative multiplayer simulation
- rollback / lag compensation
- distributed inventory/game-state ownership
- raw voice/audio streaming
- dedicated online backend beyond local/session snapshot model

Практическое правило для следующих проходов:

- если фича касается launcher/runtime/session/presence/service-shell, она может жить в `Lanline`
- если фича требует сетевой синхронизации боёвки, физики, AI или полноценного co-op state authority, это уже отдельный multiplayer stack, а не `Lanline`

Техническое следствие:

- `Lanline` можно дальше чистить и расширять как продуктовый UX/session слой
- код не должен маскировать под `Lanline` то, что на самом деле является будущей сетевой игрой

